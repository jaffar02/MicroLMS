app:
  jwt:
    secret: mySuperSecretKeyForJWTs
    expirationMs: 86400000  # 24h

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/minilmsdb
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect